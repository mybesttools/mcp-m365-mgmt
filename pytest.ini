[pytest]
asyncio_mode = auto
pythonpath = .
# Scope discovery to tests/ -- test_client.py and chat_test.py at the repo
# root are legacy manual/interactive scripts (they make live Graph API calls
# on import), not automated tests. Their test_*.py name otherwise makes
# pytest's default discovery collect and execute them.
testpaths = tests
